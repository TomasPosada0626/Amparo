# Datos privados (no deben subirse al repo)
private/

# Borradores de la wiki (M2-M5): se editan localmente y solo se suben
# a la wiki de GitHub cuando cada milestone este listo para entrega.
wiki-drafts/
# Salidas generadas (pueden contener contenido derivado del dataset privado)
artifacts/

# Entornos Python
.venv/
__pycache__/
*.pyc
.env

# Cachés y artefactos locales
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/

# SO
.DS_Store
Thumbs.db

# Corpus RAG (M3): data/corpus/normas/ SI se versiona (decision del equipo, y
# son textos de dominio publico). Lo que no se versiona es la salida generada
# por el ingest, que se puede reconstruir con pipeline.build_index().
data/rag_corpus_processed.jsonl
